export interface SpotifyAuthorization {
    access_token: string
    token_type: string
    expires_in: number
}

export interface TwitchAuthorization {
    access_token: string
    token_type: string
    expires_in: number
}